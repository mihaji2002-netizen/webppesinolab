"use client";

/**
 * A fixed film-grain overlay. Subtle, cinematic — the kind of texture that
 * separates a premium surface from a flat one. Non-interactive.
 */
export function Grain() {
  return <div className="noise" aria-hidden="true" />;
}
